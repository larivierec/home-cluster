---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: metallb
  namespace: metallb-system
spec:
  interval: 5m
  chart:
    spec:
      chart: metallb
      version: 0.13.10
      sourceRef:
        name: metallb
        namespace: flux-system
        kind: HelmRepository
      interval: 5m
  values:
    crds:
      enabled: true
    prometheusRule:
      enabled: true
    speaker:
      frr:
        enabled: false