---
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: postgresql-config-v1
  namespace: system
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Gi
  storageClassName: nfs-provision-csi
