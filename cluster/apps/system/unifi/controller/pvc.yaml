---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: unifi-config
  namespace: system
spec:
  storageClassName: nfs-minio
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi