---
apiVersion: v1
kind: PersistentVolume
metadata:
  name: data-volume
  namespace: default
spec:
  storageClassName: "manual"
  capacity:
    storage: 50Gi
  accessModes:
    - ReadWriteMany
  hostPath:
    path: "/home/christopher/persistence"
