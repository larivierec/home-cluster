---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: plex-csi
  namespace: default
spec:
  storageClassName: nfs-provision-csi
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 50Gi
