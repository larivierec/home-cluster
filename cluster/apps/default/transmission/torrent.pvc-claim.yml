---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: "torrent"
  namespace: default
spec:
  storageClassName: "manual"
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: "500Gi"
---
