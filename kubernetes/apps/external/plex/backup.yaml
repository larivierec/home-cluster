---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: plex-backup
  namespace: default
spec:
  storageClassName: nfs-minio
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Gi
