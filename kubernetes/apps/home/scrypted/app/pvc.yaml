---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: scrypted
  namespace: home
spec:
  accessModes: ["ReadWriteOnce"]
  resources:
    requests:
      storage: 10Gi
  storageClassName: longhorn
