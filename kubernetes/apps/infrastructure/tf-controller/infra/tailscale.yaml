---
apiVersion: source.toolkit.fluxcd.io/v1
kind: OCIRepository
metadata:
  name: oci-terraform-tailscale
  namespace: infrastructure
spec:
  interval: 1m
  url: oci://ghcr.io/larivierec/terraform/tailscale
  ref:
    tag: main
---
apiVersion: infra.contrib.fluxcd.io/v1alpha1
kind: Terraform
metadata:
  name: tailscale
  namespace: infrastructure
spec:
  interval: 12h
  approvePlan: "auto"
  backendConfig:
    disable: true
  cliConfigSecretRef:
    name: tfe-cli-config
  path: ./
  sourceRef:
    kind: OCIRepository
    name: oci-terraform-tailscale
    namespace: infrastructure
  tfstate:
    forceUnlock: "no"
  runnerPodTemplate:
    spec:
      volumeMounts:
        - name: tf-controller-sops-key
          mountPath: /home/runner/.config/sops/age/keys.txt
          subPath: keys.txt
      volumes:
        - name: tf-controller-sops-key
          secret:
            secretName: sops-age
            items:
              - key: age.key
                path: keys.txt
