---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: config-radarr-1
  namespace: default
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 5Gi
  storageClassName: ceph-filesystem
