---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: config-readarr-1
  namespace: default
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 3Gi
  storageClassName: longhorn-config
