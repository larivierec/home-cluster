---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: config-tautulli-0
  namespace: default
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 5Gi
  storageClassName: ceph-filesystem
