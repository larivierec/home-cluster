---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: config-sonarr-1
  namespace: default
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 7Gi
  storageClassName: ceph-filesystem
