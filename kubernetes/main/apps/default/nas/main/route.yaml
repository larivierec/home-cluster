---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: nas-route
  namespace: default
spec:
  hostnames:
    - nas.garb.dev
  parentRefs:
    - name: envoy-internal
      namespace: networking
      sectionName: https
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - group: gateway.envoyproxy.io
          kind: Backend
          name: nas-backend
---
apiVersion: gateway.envoyproxy.io/v1alpha1
kind: Backend
metadata:
  name: nas-backend
  namespace: default
spec:
  endpoints:
    - fqdn:
        hostname: nas.default.svc.cluster.local
        port: 5001
  tls:
    insecureSkipVerify: true
---
apiVersion: gateway.networking.k8s.io/v1
kind: BackendTLSPolicy
metadata:
  name: nas
  namespace: default
spec:
  targetRefs:
    - group: gateway.envoyproxy.io
      kind: Backend
      name: nas-backend
  validation:
    wellKnownCACertificates: "System"
    hostname: nas.garb.dev
  options:
    minVersion: "1.2"
    maxVersion: "1.3"
