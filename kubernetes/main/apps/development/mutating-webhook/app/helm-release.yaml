---
# yaml-language-server: $schema=https://github.com/fluxcd-community/flux2-schemas/raw/main/helmrelease-helm-v2.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: harbor-webhook
  namespace: development
spec:
  interval: 30m
  chart:
    spec:
      chart: harbor-container-webhook
      version: 0.7.1
      sourceRef:
        kind: HelmRepository
        name: indeed-eng
        namespace: flux-system
  driftDetection:
    mode: enabled
  install:
    remediation:
      retries: 3
  upgrade:
    cleanupOnFail: true
    remediation:
      strategy: rollback
      retries: 3
  values:
    rules:
      - name: "docker.io rewrite rule"
        matches:
          - "^docker.io"
        replace: "harbor.garb.dev/docker.io"
        checkUpstream: true
      - name: "ghcr.io rewrite"
        matches:
          - "^ghcr.io"
        excludes:
          - "^ghcr.io/(cloudnative-pg/)?(cloudnative-pg|postgresql):.*$"
        replace: "harbor.garb.dev/ghcr.io"
        checkUpstream: true
      - name: "quay.io rewrite"
        matches:
          - "^quay.io"
        replace: "harbor.garb.dev/quay.io"
        checkUpstream: true
